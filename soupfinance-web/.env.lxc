# LXC Backend Environment Configuration
#
# Backend container: soupfinance-backend
# Container IP: 10.115.213.183
# Backend service: Grails bootRun
# Database: MariaDB (10.115.213.114)
#
# This file configures Vite to proxy API requests to the LXC backend container.
# Usage: npm run dev -- --mode lxc

# Proxy target - where Vite forwards /rest, /client, and /account requests
VITE_PROXY_TARGET=http://10.115.213.183:9090

# API URL for frontend - use relative path so requests go through Vite proxy
# DO NOT use full backend URL here - that bypasses proxy and causes CORS issues
VITE_API_URL=/rest

# ==============================================================================
# API Consumer Credentials
# ==============================================================================
# CREDENTIALS ARE IN .env.lxc.local (git-ignored)
#
# Vite loads .env.lxc.local after this file, so credentials defined there
# will override these placeholders. This keeps secrets out of git.
#
# To set up: Copy .env.lxc.local.example to .env.lxc.local and fill in values
# See docs/PLAN-api-consumer-email-confirmation.md for credential details.
#
# Placeholder values (overridden by .env.lxc.local):
VITE_API_CONSUMER_ID=
VITE_API_CONSUMER_SECRET=
